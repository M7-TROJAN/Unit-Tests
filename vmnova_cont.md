
# 🚀 الدليل الشامل للمساهمة في مشروع VMNova

هذا الدليل يلخص الخطوات المتبعة من لحظة استلام "التاسك" وحتى دمج الكود (Merge) بنجاح.

## الخطوة الأولى: تجهيز البيئة المحلية (Local)

*هذه الخطوة تضمن أنك تعمل على أحدث كود لتقليل فرص حدوث "Conflicts".*

1. **الانتقال لبرانش التطوير:**
```bash
git checkout develop

```


2. **سحب آخر التحديثات:**
```bash
git pull origin develop

```



---

## الخطوة الثانية: إدارة المهام على GitLab

*قبل كتابة أي كود، يجب توثيق ما ستفعله.*

1. **إنشاء Issue جديدة:**
* اذهب إلى GitLab -> Issues -> New Issue.
* **العنوان:** يجب أن يبدأ بـ ID مثل `VAP-114: Secure Myrtille Integration`.
* **الوصف:** استخدم نظام **Acceptance Criteria** (مربعات اختيار) لكل المطلوب منك.


2. **تحديد الـ Milestone والـ Labels:**
* اختر الـ Milestone الحالي (مثلاً Milestone 7).
* أضف Labels مثل `feature` أو `backend`.



---

## الخطوة الثالثة: إنشاء برانش العمل (Feature Branch)

*الالتزام بنظام التسمية الموجود في ملف `CONTRIBUTING.md`.*

1. **إنشاء البرانش:**
```bash
# الصيغة: <type>/<ISSUE-ID>-<description>
git checkout -b feature/VAP-114-secure-myrtille-token

```



---

## الخطوة الرابعة: العمل على الكود والتوثيق

*الآن تبدأ مرحلة البرمجة الحقيقية.*

1. **التطوير التقني:** تنفيذ المطلوب (مثلاً عمل الـ Token Logic اللي طلبه عمر).
2. **تعديل الـ Configuration:** تحديث ملف `appsettings.json` في الـ ApiGateway لإضافة خدمتك الجديدة.
3. **التوثيق (README):**
* تحديث عدد المشاريع في ملف الـ Backend README.
* إضافة بورتات الخدمة (5070/5071) في جدول الـ Endpoints.
* إضافة دليل تكامل في مجلد `docs/`.



---

## الخطوة الخامسة: حفظ التغييرات (Commits)

*الالتزام بصيغة الكوميت الاحترافية.*

1. **فحص التغييرات:**
```bash
git status

```


2. **إضافة الملفات:**
```bash
git add .

```


3. **الـ Commit:**
```bash
# الصيغة: <ISSUE-ID>: <Description>
git commit -m "VAP-114: Implement token-based secure VM access

- Created ConnectionToken entity
- Added token validation logic
- Updated README with ports 5070/5071"

```



---

## الخطوة السادسة: رفع الكود وعمل الـ Merge Request

*المرحلة النهائية لعرض شغلك للمراجعة.*

1. **رفع البرانش للـ Remote:**
```bash
git push origin feature/VAP-114-secure-myrtille-token

```


2. **إنشاء Merge Request (MR):**
* اذهب لـ GitLab واضغط **Create Merge Request**.
* **Target Branch:** تأكد أنه `develop`.
* **Description:** استخدم الـ Template الاحترافي (Summary, Changes, Checklist).
* **الربط:** اكتب `Closes #114` في الوصف ليتم إغلاق الـ Issue تلقائياً عند القبول.



---

## 💡 نصائح ذهبية للتعامل مع المهندس عمر:

* **Checklists:** هو يحب رؤية مربعات الاختيار `[x]` في الـ Issue والـ MR، لأنها تعكس دقة المهندس.
* **Architecture:** دائماً تأكد أن الكود يتبع الـ **Clean Architecture Layers** (Domain -> Application -> Infrastructure -> API).
* **Ports:** لا تنسَ أبداً تحديث جدول الـ Endpoints في ملف الـ README؛ بالنسبة له، الكود بدون توثيق البورت هو كود "ناقص".
* **Patterns:** قبل تنفيذ أي شيء، انظر كيف نُفذ في خدمات أخرى (مثل Identity أو VirtualMachine) وقلده بالحرف في التنسيق.

**ما هي الخطوة التي تود أن نتعمق فيها أكثر أو نطبقها عملياً الآن؟**
